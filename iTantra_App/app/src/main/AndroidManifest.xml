<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Microphone audio recording for PTT -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Wi-Fi Direct discovery and connection on Android 13+ (API 33+) -->
    <!-- usesPermissionFlags="neverForLocation" strongly asserts that location is not derived from Wi-Fi P2P,
         eliminating the requirement for runtime ACCESS_FINE_LOCATION on Android 13, 14, and 15 -->
    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="tiramisu" />

    <!-- Wi-Fi Direct peer discovery fallback for Android 8.0 - 12L (API 26-32) -->
    <!-- maxSdkVersion="32" ensures fine/coarse location permissions are NOT requested or granted on Android 13+ -->
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="32" />
    <uses-permission
        android:name="android.permission.ACCESS_COARSE_LOCATION"
        android:maxSdkVersion="32" />

    <!-- Wi-Fi State permissions for WifiP2pManager operations (normal protection level) -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- TCP socket and network state permissions for local P2P networking (normal protection level) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />

    <!-- Wi-Fi Direct hardware feature declaration (optional flag allows installation on non-P2P test devices) -->
    <uses-feature
        android:name="android.hardware.wifi.direct"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/ic_dialog_info"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/ic_dialog_info"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
